torch==2.0.1  # PyTorch for model, preprocessing, tensor manipulation
torchvision==0.15.2  # For using pre-trained DenseNet models
fastai==2.7.12  # Fastai for learner, datablocks, callbacks
scikit-learn==1.3.0  # Metrics like ROC AUC, confusion matrix, train/test split
pandas==2.0.3  # Dataframes for loading and processing CSV files
numpy==1.25.2  # Numerical operations for preprocessing
Pillow==9.2.0  # Image loading and manipulation (used in preprocessing and utils)
matplotlib==3.7.2  # For visualizing image patches
zipfile38  # Handling zip files (if required separately)
